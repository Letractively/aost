application.title=tellurium
application.vendor=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form,**/*.groovy
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
buildfile=nbbuild.xml
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/tellurium-${version}.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.out-production=out\\production\\
file.reference.out-test=out\\test\\
file.reference.src-main=src/main
file.reference.src-test=src/test
includes=**
jar.compress=false
javac.classpath=\
    ${libs.groovy-all-1.5.6.classpath}:\
    ${libs.selenium-server.classpath}:\
    ${libs.selenium-java-client-driver.classpath}:\
    ${file.reference.out-production}:\
    ${file.reference.out-test}:\
    ${libs.junit_4.classpath}:\
    ${libs.testng-5.7-jdk15.jar.classpath}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
main.class=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.groovy.dir=src\\main\\groovy
src.resources.dir=src\\main\\resources
test.groovy.dir=src\\test\\groovy
test.resources.dir=src\\test\\resources
version = 0.4.0
